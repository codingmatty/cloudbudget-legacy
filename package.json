{
  "name": "CloudBudget",
  "version": "0.1.2",
  "description": "A simple Budgeting Web App",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf public/*",
    "prebuild": "npm run clean -s",
    "build": "npm run build:scripts -s && npm run build:styles -s && npm run build:markup -s",
    "build:scripts": "browserify -t debowerify -d source/scripts/cloudbudget.js  > public/main.js",
    "prebuild:styles": "mkdir -p public/assets/bootstrap && ncp bower_components/bootstrap/dist/ public/assets/bootstrap/",
    "build:styles": "node-sass source/styles/index.scss -o build && cleancss -o public/main.min.css build/*.css",
    "postbuild:styles": "rimraf build",
    "build:markup": "ncp source/markup/ public/",
    "test": "karma start --singleRun",
    "watch:test": "karma start",
    "prewatch": "npm run clean -s",
    "watch": "node bin/watch",
    "livereload": "live-reload --port 9091 public/",
    "dev": "node bin/www",
    "start": "npm run build && node server/server.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/swengmatt/cloudbudget.git"
  },
  "author": "Matthew Jacobs",
  "license": "MIT",
  "dependencies": {
    "bower": "latest",
    "body-parser": "latest",
    "browserify": "^10.2.4",
    "clean-css": "^3.3.4",
    "debowerify": "^1.3.1",
    "debug": "^2.2.0",
    "express": "latest",
    "lodash": "latest",
    "mongoose": "latest",
    "ncp": "^2.0.0",
    "node-sass": "^3.2.0",
    "rimraf": "^2.4.0"
  },
  "devDependencies": {
    "exorcist": "latest",
    "hashmark": "latest",
    "http-server": "latest",
    "jade": "latest",
    "jshint": "latest",
    "karma": "latest",
    "karma-browserify": "latest",
    "karma-cli": "latest",
    "karma-mocha": "latest",
    "karma-phantomjs-launcher": "latest",
    "live-reload": "latest",
    "minifyify": "latest",
    "mocha": "latest",
    "nodemon": "latest",
    "watch": "latest"
  }
}
